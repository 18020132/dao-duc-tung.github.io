---
title: Categories
---

<h1>Categories</h1>

<ul>
  <% site.categories.forEach((category) => { %>
  <li>
    <%= category.title %> <% category.articles.forEach((article) => { %>
    <ul>
      <li><a href="<%= site.basePath %>/<%= article.url %>"><%= article.title %></a></li>
    </ul>
    <% }) %>
  </li>
  <% }) %>
</ul>
